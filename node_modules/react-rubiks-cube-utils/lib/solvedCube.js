"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.solvedCube = void 0;
function generateSolvedCube(n) {
    const colors = {
        U: 'W',
        D: 'Y',
        L: 'O',
        R: 'R',
        F: 'G',
        B: 'B',
    };
    const cube = {
        U: [],
        D: [],
        L: [],
        R: [],
        F: [],
        B: [],
    };
    for (const face in cube) {
        if (cube.hasOwnProperty(face)) {
            for (let i = 0; i < n; i++) {
                cube[face].push(Array(n).fill(colors[face]));
            }
        }
    }
    return cube;
}
const cubeTypeToSize = {
    '2x2': 2,
    '3x3': 3,
    '4x4': 4,
    '5x5': 5,
    '6x6': 6,
    '7x7': 7,
};
function solvedCube({ type }) {
    const size = cubeTypeToSize[type];
    if (!size) {
        throw new Error(`Unsupported cube type: ${type}`);
    }
    return generateSolvedCube(size);
}
exports.solvedCube = solvedCube;
